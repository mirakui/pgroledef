version: "2"
linters:
  default: standard
  enable:
    - misspell
  settings:
    errcheck:
      exclude-functions:
        - (*github.com/jackc/pgx/v5.Conn).Close
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
